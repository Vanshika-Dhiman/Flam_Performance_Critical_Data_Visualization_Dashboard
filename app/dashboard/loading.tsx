import DashboardSkeleton from '@/components/DashboardSkeleton';

/** Route-level loading UI shown instantly during client-side navigation to /dashboard. */
export default function Loading() {
  return <DashboardSkeleton />;
}
